#include<stdio.h>
int main()
{
    int i,arr[100],n,pos;
    printf("Enter element:");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    printf("Enter key to in")
}